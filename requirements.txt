# create a branch
git checkout -b chore/requirements-compatible-pins

# write the new requirements.txt
cat > requirements.txt <<'REQ'
# Web framework & server
fastapi~=0.111
uvicorn[standard]~=0.30

# HTTP / websockets
httpx~=0.28
websockets~=12.0

# Data & validation
pydantic~=2.8
orjson~=3.11

# Templates
jinja2~=3.1

# Database (async Postgres)
asyncpg~=0.29

# Time utils
python-dateutil~=2.9

# OpenAI SDK
openai>=1.40.0

# Env config
python-dotenv~=1.0
REQ

git add requirements.txt
git commit -m "chore: switch to compatible pins; add asyncpg/dateutil; drop psycopg2-binary & ujson"
git push -u origin chore/requirements-compatible-pins
