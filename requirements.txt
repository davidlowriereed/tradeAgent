# Web framework & server
fastapi~=0.111
uvicorn[standard]~=0.30

# HTTP / websockets
httpx~=0.28
websockets~=12.0

# Data & validation
pydantic~=2.8
orjson~=3.11

# Templates (remove if unused)
jinja2~=3.1

# Database (async Postgres)
asyncpg~=0.29

# Time utils
python-dateutil~=2.9

# OpenAI SDK (cap major for safety)
openai>=1.40,<2

# Env config
python-dotenv~=1.0
